import React from "react";

const AlertSucces = ({ text }) => {
    return (
        <div className='alert alert-success' role='alert'>
            {text}
        </div>
    );
};

export default AlertSucces;
